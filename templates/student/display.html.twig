<center>
    <table border="1">
        <tr>
            <th>name</th>
            <th>age</th>
            <th>Actions</th>
        </tr>
        {% for i in students %}
            <tr>
                <td>{{ i.name }}</td>
                <td>{{ i.age }}</td>
                <td><a href="{{ path('delete_student',{'id':i.id}) }}">delete</a> <a href="{{ path('modify_student',{'id':i.id}) }}">modify</a></td>
            </tr>
        {% endfor %}
    </table>

    {{ form(form) }}
</center>