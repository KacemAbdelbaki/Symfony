<center>
    <table border="1">
        <tr>
            <th>Name</th>
            <th>age</th>
            <th>Phone Num</th>
            <th>Address</th>
            <th>Actions</th>
        </tr>
        {% for p in people %}
            <tr>
                <td>{{ p.name }}</td>
                <td>{{ p.age }}</td>
                <td>{{ p.phoneNumber }}</td>
                <td>{{ p.address }}</td>
                <td> <a href="{{ path('Pers_Modify',{'id':p.id}) }}">edit</a> <a href="{{ path('Pers_Delete',{'id':p.id}) }}">delete</a> </td>
            </tr>
        {% endfor %}
    </table></br>
    {{ form(form) }}
</center>